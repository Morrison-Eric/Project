Система для мониторинга производительности и генерации отчетов
=================
# Возможности
=================
- Сбор метрик: CPU, память, диск, сеть, системная информация
- Визуализация: автоматическое создание графиков и диаграмм
- Отчеты: генерация отчетов в текстовом, HTML и JSON форматах
- Планирование: автоматические ежедневные/еженедельные отчеты
- Мониторинг: проверка превышения пороговых значений
- Архивация: хранение истории отчетов
=================
# Установка
=================
1. Клонируйте репозиторий:
git clone https://github.com/ваше_имя/performance-reports.git
cd performance-reports

2. Установите зависимости:
pip install -r requirements.txt

3.Создайте необходимые директории:
python -c "from config import ensure_directories; ensure_directories()"
=================
# Использование
=================
1. Сбор метрик
# Собрать 10 метрик с интервалом в 1 секунду
python main.py collect -n 10 -i 1
# Собрать 60 метрик для часового отчета
python main.py collect -n 60 -i 1 -o hourly_metrics.json

2. Генерация отчетов
# Текстовый отчет
python main.py report -t text -f metrics.json
# HTML отчет
python main.py report -t html -f metrics.json -o report.html
# JSON отчет
python main.py report -t json -f metrics.json -o summary.json

3. Визуализация
# График загрузки CPU
python main.py visualize -t cpu -f metrics.json
# Все графики
python main.py visualize -t all -f metrics.json -o comprehensive.png

4. Планирование
# Ежедневные отчеты (один раз)
python main.py schedule daily
# Еженедельные отчеты (непрерывно)
python main.py schedule weekly -c




----------------------------------------------------------------


# Конфигурация
Основные настройки в config.py:

DEFAULT_CONFIG = {
    'metrics': {
        'cpu': True,           # Сбор метрик CPU
        'memory': True,        # Сбор метрик памяти
        'disk': True,         # Сбор метрик диска
        'network': True,      # Сбор сетевых метрик
    },
    'thresholds': {
        'cpu_warning': 80,    # Порог предупреждения для CPU (%)
        'memory_warning': 85, # Порог для памяти (%)
        'disk_warning': 90    # Порог для диска (%)
    }
}


----------------------------------------------------------------


# Пример отчета:
============================================================
ОТЧЕТ О ПРОИЗВОДИТЕЛЬНОСТИ СИСТЕМЫ
============================================================
Сгенерирован: 2026-01-01 14:30:00
Период измерений: 60 записей

ЗАГРУЗКА CPU:
  Общая загрузка: 45.2%
  Ядер: 8
  Частота: 3200 МГц

ПАМЯТЬ:
  Оперативная память: 67.5%
  Использовано: 8.2 ГБ
  Всего: 16.0 ГБ
  Своп: 12.3%

ПРОВЕРКА ПОРОГОВ:
 Все показатели в норме


----------------------------------------------------------------


Отчет включает цветовую индикацию:

Зеленый: показатели в норме

Желтый: приближение к порогу

Красный: превышение порога


----------------------------------------------------------------
